[project]
name = "ty-mwe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

# Since we're using Zed, this is not needed.
# [dependency-groups]
# dev = [
#     "ty>=0.0.1a25",
# ]
